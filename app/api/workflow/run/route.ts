import { NextRequest, NextResponse } from 'next/server'; import { crawlDueSources } from '@/lib/news'; import { runPipeline } from '@/lib/pipeline'; import { sql } from '@/lib/db';
export async function POST(req:NextRequest){ const b=await req.json().catch(()=>({})); if(b.crawl) await crawlDueSources(); const articleId=b.articleId || (sql.prepare("select id from articles where status in ('new','scripted') order by id desc limit 1").get() as any)?.id; if(!articleId) return NextResponse.json({ok:false,message:'Keine News gefunden'}); const video=await runPipeline(Number(articleId)); return NextResponse.json({ok:true,articleId,video}); }
