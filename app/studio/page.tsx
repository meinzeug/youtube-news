export const dynamic = 'force-dynamic';
async function run(){'use server'; const {crawlDueSources}=await import('@/lib/news'); const {runPipeline}=await import('@/lib/pipeline'); const {sql}=await import('@/lib/db'); await crawlDueSources(); const row=sql.prepare("select id from articles where status in ('new','scripted') order by id desc limit 1").get() as any; if(row) await runPipeline(row.id);}
export default function Studio(){return <main className="page"><h1>Automations-Studio</h1><div className="card"><p>Startet Crawl + komplette Produktionspipeline. Für Cron: POST /api/workflow/run mit crawl=true.</p><form action={run}><button>Pipeline starten</button></form></div><div className="grid"><div className="card"><h3>1. Crawl</h3><p>RSS/HTML Quellen werden dedupliziert.</p></div><div className="card"><h3>2. KI Skript</h3><p>OpenRouter schreibt originellen deutschen Sprechertext.</p></div><div className="card"><h3>3. Audio & Bild</h3><p>ElevenLabs oder lokaler FFmpeg-Fallback; SVG-Bildgenerator ohne Binärdateien.</p></div><div className="card"><h3>4. Video & Upload</h3><p>FFmpeg rendert MP4, YouTube Upload-API ist als Integrationspunkt vorbereitet.</p></div></div></main>}
